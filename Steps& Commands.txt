-- install vagrant tool ==> https://developer.hashicorp.com/vagrant/downloads
-- open the directory of the Vagrantfile 
-- open the git bash and run the below
vagrant up

vagrant ssh

systemctl status jenkins

-- copy your IP and open in browser "IPv4:8080"

cat /var/lib/jenkins/secrets/initialAdminPassword

-- Back to browser and continue installation process

-- Will need to install Java 8 version for maven
sudo apt-get install -y openjdk-11-jdk

ls  /usr/lib/jvm/ -- To check the Java installed 

-- Copy java 8 path 
 /usr/lib/jvm/java-8-openjdk-amd64/

-- open Dashboard and go to Manage Jenkins then go to Global Tool Configuration
-- then go to JDK Installations
-- add name as OracleJDK8
-- then paste the java 8 path at Java_Home 
-- go throw in the page to Maven section
-- add name and choose the version
-- Then click on save
#Jenkins not actaully install the tools when you run the bulid job that time when ever it needs that time it will install

